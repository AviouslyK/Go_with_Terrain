using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Game : MonoBehaviour
{
    public GameObject wStone;
    public GameObject bStone;

    private GameObject[] BlackStones = new GameObject[41]; // holds black's stones
    private GameObject[] WhiteStones = new GameObject[41]; // holds white's stones


    // Start is called before the first frame update
    void Start()
    {
        // Put all black and white stones on the side of the board 
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
